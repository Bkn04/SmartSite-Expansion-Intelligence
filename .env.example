# Foursquare Places API – Free tier (100,000 calls/month)
# Sign up at https://foursquare.com/developers/
# 1. Create a new project → get an API Key
# 2. Copy this file to .env and fill in your key
# 3. Restart the dev server (npm run dev)

VITE_FOURSQUARE_API_KEY=your_foursquare_api_key_here

# Without this key, competitor data will fall back to OpenStreetMap (less complete).
# MTA ridership data works without any key – it's bundled into the app.
